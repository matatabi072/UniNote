[workspace]
resolver = "2"
members = [
    "uninote",
    "sidecars/uninote-sync-todoist",
    "sidecars/uninote-sync-gtasks",
]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
